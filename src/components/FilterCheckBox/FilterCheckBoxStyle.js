import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    checkboxContainer: {
        marginTop: 10,
        marginLeft: 15,
        marginBottom: 25
    },
    checkbox: {
        padding: 12,
        borderRadius: 8
    },
});

export default styles;